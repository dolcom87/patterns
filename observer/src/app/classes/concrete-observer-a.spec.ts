import { ConcreteObserverA } from './concrete-observer-a';

describe('ConcreteObserverA', () => {
  it('should create an instance', () => {
    expect(new ConcreteObserverA()).toBeTruthy();
  });
});
